[project]
name = "sdlc-worker-tester"
version = "0.1.0"
description = "Tester worker — reads from Service Bus repo-index queue and writes hello.txt to Storage"
requires-python = ">=3.13"
dependencies = [
    "azure-identity>=1.25.3",
    "azure-servicebus>=7.14.2",
    "azure-storage-blob>=12.25.1",
]
