name: Deploy sdlc-worker-tester

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      env:
        description: "Environment to deploy to"
        required: true
        type: choice
        options:
          - DEV
          - PROD

jobs:
  deploy:
    name: "Deploy → ${{ inputs.env || 'DEV' }}"
    runs-on: ubuntu-latest

    env:
      ARM_CLIENT_ID:       ${{ secrets[format('SDLC_{0}_AZURE_CLIENT_ID_TERRAFORM', inputs.env || 'DEV')] }}
      ARM_CLIENT_SECRET:   ${{ secrets[format('SDLC_{0}_AZURE_CLIENT_SECRET_TERRAFORM', inputs.env || 'DEV')] }}
      ARM_TENANT_ID:       ${{ secrets[format('SDLC_{0}_AZURE_TENANT_ID_TERRAFORM', inputs.env || 'DEV')] }}
      ARM_SUBSCRIPTION_ID: ${{ secrets[format('SDLC_{0}_AZURE_SUBSCRIPTION_ID_TERRAFORM', inputs.env || 'DEV')] }}

    steps:
      - uses: actions/checkout@v4

      - name: Set env vars
        run: |
          ENV_INPUT="${{ inputs.env || 'DEV' }}"
          ENV_LOWER=$(echo "$ENV_INPUT" | tr '[:upper:]' '[:lower:]')
          echo "ENV_LOWER=$ENV_LOWER"             >> $GITHUB_ENV
          echo "ACR_NAME=crsdlc${ENV_LOWER}"      >> $GITHUB_ENV
          echo "ACR_SERVER=crsdlc${ENV_LOWER}.azurecr.io" >> $GITHUB_ENV
          echo "SHORT_SHA=$(echo '${{ github.sha }}' | cut -c1-7)" >> $GITHUB_ENV

      - name: Azure Login
        run: |
          az login --service-principal \
            --username "$ARM_CLIENT_ID" \
            --password "$ARM_CLIENT_SECRET" \
            --tenant "$ARM_TENANT_ID"
          az account set --subscription "$ARM_SUBSCRIPTION_ID"

      - name: Build and push image to ACR
        run: |
          IMAGE="${{ env.ACR_SERVER }}/sdlc-worker-tester:${{ env.SHORT_SHA }}"
          az acr login --name "${{ env.ACR_NAME }}"
          docker build -t "$IMAGE" .
          docker push "$IMAGE"
          echo "IMAGE=$IMAGE" >> $GITHUB_ENV

      - name: Configure ACR registry auth on Container App
        run: |
          SHARED_MI_ID=$(az identity show \
            --name "id-sdlc-shared-${{ env.ENV_LOWER }}" \
            --resource-group "rg-sdlc-shared-${{ env.ENV_LOWER }}" \
            --query id -o tsv)
          az containerapp registry set \
            --name "ca-sdlc-tester-${{ env.ENV_LOWER }}" \
            --resource-group "rg-sdlc-base-${{ env.ENV_LOWER }}" \
            --server "${{ env.ACR_SERVER }}" \
            --identity "$SHARED_MI_ID"

      - name: Deploy to Container App
        run: |
          az containerapp update \
            --name ca-sdlc-tester-dev \
            --resource-group rg-sdlc-base-dev \
            --image "${{ env.IMAGE }}"
